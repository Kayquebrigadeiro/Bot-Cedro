# Persistência de dados para o Render
# O arquivo cedro.db será salvo no armazenamento persistente

# Configure no Render:
# 1. Vá em "Storage" no seu Web Service
# 2. Adicione um Disk com path: /opt/render/project/src
# 3. O banco de dados será salvo automaticamente

# O arquivo .gitignore já está configurado para não enviar o cedro.db
